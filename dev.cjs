const http=require('http'),fs=require('fs');const port=Number(process.env.PORT||5173);http.createServer((req,res)=>{const p=new URL(req.url,'http://localhost').pathname;if(!['/','/index.html','/control','/display'].includes(p)){res.writeHead(404);res.end('Not found');return;}res.writeHead(200,{'Content-Type':'text/html; charset=utf-8','Cache-Control':'no-store'});res.end(fs.readFileSync(__dirname+'/index.html'));}).listen(port,'127.0.0.1',()=>console.log('Control: http://127.0.0.1:'+port+'/control\nDisplay: http://127.0.0.1:'+port+'/display'));